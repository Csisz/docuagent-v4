/**
 * Invoice verification form.
 * Left: AI-extracted fields (editable)
 * Right: source email/document preview
 * Bottom: approval action buttons
 * TODO: connect to PUT /invoice/:id + POST /invoice/:id/verify
 */
export function InvoiceVerify({ invoiceId }) {
  return <div>TODO: InvoiceVerify id={invoiceId}</div>
}

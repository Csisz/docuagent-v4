/**
 * Document Agent â€” main page scaffold.
 * TODO:
 *   - DocumentLibrary (upload + list)
 *   - DocumentChat (RAG interface)
 *   - DocumentDetail (summary + key data)
 */
export default function DocumentPage() {
  return (
    <div className="space-y-4">
      <h2 className="text-[16px] font-semibold text-foreground">Document Agent</h2>
      <p className="text-[13px] text-muted-foreground">TODO: DocumentLibrary, DocumentChat</p>
    </div>
  )
}

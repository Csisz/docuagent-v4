/**
 * Email Agent â€” main page scaffold.
 * TODO:
 *   - EmailList (inbox with category/status filters)
 *   - ApprovalQueue (NEEDS_ATTENTION emails)
 *   - EmailDetail (view + approve reply)
 */
export default function EmailPage() {
  return (
    <div className="space-y-4">
      <h2 className="text-[16px] font-semibold text-foreground">Email Agent</h2>
      <p className="text-[13px] text-muted-foreground">TODO: EmailList, ApprovalQueue</p>
    </div>
  )
}
